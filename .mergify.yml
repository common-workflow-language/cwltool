queue_rules:
  - name: default
    queue_conditions:
      - "#approved-reviews-by>=1"
      - -draft
      - base=main
    merge_conditions:
      - -draft # not a draft
      - base=main
    merge_method: merge

pull_request_rules:
  - name: refactored queue action rule
    conditions: []
    actions:
      queue:
